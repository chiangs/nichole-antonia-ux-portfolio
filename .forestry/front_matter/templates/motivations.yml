---
label: Motivations
hide_body: true
fields:
- name: skillbar
  type: field_group_list
  fields:
  - name: label_1
    type: text
    config:
      required: true
      min: 2
    label: Label 1
    description: First or only label
  - name: label_2
    type: text
    config:
      required: false
    label: Label 2
    description: Second label if a scale
  - name: level
    type: number
    label: Level
    description: Level from 1 - 10 (left to right)
    default: 0
    required: true
    config:
      min: 0
      max: 10
      step: 1
      required: true
  config:
    min: 
    max: 
    labelField: 
  label: Skillbar
  description: Skillbar with 1 or 2 labels.
