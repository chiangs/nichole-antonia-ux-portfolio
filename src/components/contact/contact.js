import React from "react"
import PropTypes from "prop-types"
import css from "./contact.module.css"

const contact = props => {
  return <section className={css.Contact}></section>
}

contact.propTypes = {}

export default contact
